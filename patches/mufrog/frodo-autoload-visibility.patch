--- a/Src/Display.cpp
+++ b/Src/Display.cpp
@@ -320,5 +320,5 @@
 // Manual autoload coordination variables
-static bool manual_autoload_triggered = false;
-static bool any_autoload_in_progress = false;
+bool manual_autoload_triggered = false;
+bool any_autoload_in_progress = false;
 
 void kbd_buf_feed(char *s)
--- a/libretro/core/libretro.cpp
+++ b/libretro/core/libretro.cpp
@@ -7,4 +7,6 @@
 #include "Version.h"
 
+#include "Prefs.h"
+
 #ifdef NO_LIBCO
 #include "main.h"
--- a/libretro/gui/file.cpp
+++ b/libretro/gui/file.cpp
@@ -23,1 +23,1 @@
-static int strcasecmp(const char *s1, const char *s2) {
+static int frodo_strcasecmp(const char *s1, const char *s2) {
@@ -33,1 +33,1 @@
-static char *strdup(const char *s) {
+static char *frodo_strdup(const char *s) {
@@ -107,1 +107,1 @@
-   if (!strcasecmp(&pszFileName[strlen(pszFileName)-strlen(pszExtension)], pszExtension))
+   if (!frodo_strcasecmp(&pszFileName[strlen(pszFileName)-strlen(pszExtension)], pszExtension))
@@ -196,1 +196,1 @@
-		filepath = strdup(pszFileName);
+		filepath = frodo_strdup(pszFileName);
--- a/libretro/gui/unzip.cpp
+++ b/libretro/gui/unzip.cpp
@@ -27,1 +27,1 @@
-static int strcasecmp(const char *s1, const char *s2) {
+static int frodo_unzip_strcasecmp(const char *s1, const char *s2) {
@@ -240,1 +240,1 @@
-	return strcasecmp(fileName1, fileName2);
+	return frodo_unzip_strcasecmp(fileName1, fileName2);
--- a/libretro/gui/zip.cpp
+++ b/libretro/gui/zip.cpp
@@ -23,1 +23,1 @@
-static int strncasecmp(const char *s1, const char *s2, size_t n) {
+static int frodo_zip_strncasecmp(const char *s1, const char *s2, size_t n) {
@@ -241,1 +241,1 @@
-         if (strncasecmp(zip->names[i], dir, strlen(dir)) == 0)
+         if (frodo_zip_strncasecmp(zip->names[i], dir, strlen(dir)) == 0)
@@ -253,1 +253,1 @@
-                     if (strncasecmp(temp, files->names[j], slash+1) == 0)
+                     if (frodo_zip_strncasecmp(temp, files->names[j], slash+1) == 0)
--- a/libretro/sf2000_compat.h
+++ b/libretro/sf2000_compat.h
@@ -8,1 +8,2 @@
 // Missing functions for SF2000 build
+#if !defined(__linux__)
@@ -35,3 +36,4 @@
 }
 
+#endif
 #endif // SF2000_COMPAT_H
