diff --git a/Makefile.common b/Makefile.common
index 9742c05..28783ef 100644
--- a/Makefile.common
+++ b/Makefile.common
@@ -23,7 +23,6 @@ SOURCES_C := $(CORE_DIR)/main.c \
              $(LIBRETRO_COMM_DIR)/compat/compat_posix_string.c \
              $(LIBRETRO_COMM_DIR)/compat/compat_strl.c \
              $(LIBRETRO_COMM_DIR)/compat/fopen_utf8.c \
-             $(LIBRETRO_COMM_DIR)/encodings/encoding_utf.c \
              $(LIBRETRO_COMM_DIR)/file/file_path.c \
              $(LIBRETRO_COMM_DIR)/file/file_path_io.c \
              $(LIBRETRO_COMM_DIR)/streams/file_stream.c \
diff --git a/common.h b/common.h
index d9a0f1d..4ffc8cc 100644
--- a/common.h
+++ b/common.h
@@ -40,6 +40,10 @@
   #endif
 #endif
 
+#ifdef GPSP_DYNAREC_SAFE_FALLBACK
+#define GPSP_DYNAREC_LOOKUP_FAILED 0x7ffffffeu
+#endif
+
 #define ror(dest, value, shift)                                               \
   dest = ((value) >> (shift)) | ((value) << (32 - (shift)))                   \
 
diff --git a/cpu.h b/cpu.h
index dd6cb89..6e2fe23 100644
--- a/cpu.h
+++ b/cpu.h
@@ -155,6 +155,15 @@ extern u8 ram_translation_cache[RAM_TRANSLATION_CACHE_SIZE];
 #endif
 extern u8 *rom_translation_ptr;
 extern u8 *ram_translation_ptr;
+extern u32 rom_translation_flush_count;
+extern u32 ram_translation_flush_count;
+extern u32 ram_translation_capacity_flush_count;
+extern u32 ram_translation_store_flush_count;
+extern u32 ram_translation_dma_flush_count;
+extern u32 rom_translation_peak;
+extern u32 ram_translation_peak;
+extern u32 jit_cache_sync_calls;
+extern u32 jit_cache_sync_failures;
 
 #define MAX_TRANSLATION_GATES 8
 
@@ -166,6 +173,8 @@ extern u32 rom_branch_hash[ROM_BRANCH_HASH_SIZE];
 
 void flush_translation_cache_rom(void);
 void flush_translation_cache_ram(void);
+void flush_translation_cache_ram_store(void);
+void flush_translation_cache_ram_dma(void);
 void dump_translation_cache(void);
 void init_dynarec_caches(void);
 void flush_dynarec_caches(void);
diff --git a/cpu_threaded.c b/cpu_threaded.c
index 8942ab3..0fd2134 100644
--- a/cpu_threaded.c
+++ b/cpu_threaded.c
@@ -61,6 +61,15 @@ u32 ewram_code_max =  0U;
 
 #define INITIAL_ROM_WATERMARK   16   // To avoid NULL aliasing
 u32 rom_cache_watermark = INITIAL_ROM_WATERMARK;
+u32 rom_translation_flush_count;
+u32 ram_translation_flush_count;
+u32 ram_translation_capacity_flush_count;
+u32 ram_translation_store_flush_count;
+u32 ram_translation_dma_flush_count;
+u32 rom_translation_peak;
+u32 ram_translation_peak;
+u32 jit_cache_sync_calls;
+u32 jit_cache_sync_failures;
 
 u8 *bios_swi_entrypoint = NULL;
 
@@ -247,8 +256,14 @@ u32 ewram_code_max =  0U;
     __clear_cache(baseaddr, endptr);
   }
 #elif defined(MIPS_ARCH)
+  extern int cacheflush(void *address, int bytes, int cache);
   void platform_cache_sync(void *baseaddr, void *endptr) {
-    __builtin___clear_cache(baseaddr, endptr);
+    int length = (int)((char *)endptr - (char *)baseaddr);
+    if (length <= 0)
+      return;
+    jit_cache_sync_calls++;
+    if (cacheflush(baseaddr, length, 3) != 0)
+      jit_cache_sync_failures++;
   }
 #else
   /* x86 CPUs have icache consistency checks */
@@ -2653,12 +2660,15 @@ u8 function_cc *block_lookup_address_arm(u32 pc)
   unsigned i;
   for (i = 0; i < 4; i++) {
     u8 *ret = block_lookup_translate_arm(pc);
-    if (ret) {
+    if (ret && ret != (u8 *)(~0)) {
       translate_icache_sync();
       return ret;
     }
+    if (ret == (u8 *)(~0))
+      break;
   }
 
+  reg[REG_PC] = pc;
   printf("bad jump %x (%x)\n", pc, reg[REG_PC]);
   fflush(stdout);
   return NULL;
@@ -2669,11 +2679,14 @@ u8 function_cc *block_lookup_address_thumb(u32 pc)
   unsigned i;
   for (i = 0; i < 4; i++) {
     u8 *ret = block_lookup_translate_thumb(pc);
-    if (ret) {
+    if (ret && ret != (u8 *)(~0)) {
       translate_icache_sync();
       return ret;
     }
+    if (ret == (u8 *)(~0))
+      break;
   }
+  reg[REG_PC] = pc;
   printf("bad jump %x (%x)\n", pc, reg[REG_PC]);
   fflush(stdout);
   return NULL;
@@ -2887,8 +2900,10 @@ u8 function_cc *block_lookup_address_thumb(u32 pc)
 #define MAX_BLOCK_SIZE   1024   // 2/4KiB blocks max
 #define MAX_EXITS          32   // This covers 99% blocks
 
+#if TRANSLATOR_WORKSPACE_SIZE == 0
 block_data_type block_data[MAX_BLOCK_SIZE];
 block_exit_type block_exits[MAX_EXITS];
+#endif
 
 #define smc_write_arm_yes() {                                                 \
   intptr_t offset = (pc < 0x03000000) ? 0x40000 : -0x8000;                    \
@@ -3009,6 +3024,12 @@ if (ram_region) {                                                             \
 
 bool translate_block_arm(u32 pc, bool ram_region)
 {
+#if TRANSLATOR_WORKSPACE_SIZE > 0
+  block_data_type *block_data = (block_data_type *)
+    (ram_translation_cache + RAM_TRANSLATION_CACHE_SIZE);
+  block_exit_type *block_exits = (block_exit_type *)
+    (block_data + MAX_BLOCK_SIZE);
+#endif
   u32 opcode = 0;
   u32 last_opcode;
   u32 condition;
@@ -3102,9 +3123,10 @@ bool translate_block_arm(u32 pc, bool ram_region)
        the beginning. */
 
     if(translation_ptr > translation_cache_limit) {
-      if (ram_region)
+      if (ram_region) {
+        ram_translation_capacity_flush_count++;
         flush_translation_cache_ram();
-      else
+      } else
         flush_translation_cache_rom();
       return false;
     }
@@ -3174,6 +3196,12 @@ bool translate_block_arm(u32 pc, bool ram_region)
 
 bool translate_block_thumb(u32 pc, bool ram_region)
 {
+#if TRANSLATOR_WORKSPACE_SIZE > 0
+  block_data_type *block_data = (block_data_type *)
+    (ram_translation_cache + RAM_TRANSLATION_CACHE_SIZE);
+  block_exit_type *block_exits = (block_exit_type *)
+    (block_data + MAX_BLOCK_SIZE);
+#endif
   u32 opcode = 0;
   u32 last_opcode;
   u32 condition;
@@ -3264,9 +3292,10 @@ bool translate_block_thumb(u32 pc, bool ram_region)
 
     if(translation_ptr > translation_cache_limit)
     {
-      if (ram_region)
+      if (ram_region) {
+        ram_translation_capacity_flush_count++;
         flush_translation_cache_ram();
-      else
+      } else
         flush_translation_cache_rom();
       return false;
     }
@@ -3341,6 +3370,12 @@ void init_bios_hooks(void)
 
 void flush_translation_cache_ram(void)
 {
+  u32 used = (u32)(ram_translation_ptr - ram_translation_cache);
+
+  if (used > ram_translation_peak)
+    ram_translation_peak = used;
+  ram_translation_flush_count++;
+
   /* Flushes RAM caches avoiding doing too much work (ie. wiping unused memory) */
   flush_ram_count++;
   /*printf("ram flush %d (pc %x), %x to %x, %x to %x\n",
@@ -3377,8 +3412,26 @@ void flush_translation_cache_ram(void)
   ram_block_tag = INITIAL_TOP_TAG;
 }
 
+void flush_translation_cache_ram_store(void)
+{
+  ram_translation_store_flush_count++;
+  flush_translation_cache_ram();
+}
+
+void flush_translation_cache_ram_dma(void)
+{
+  ram_translation_dma_flush_count++;
+  flush_translation_cache_ram();
+}
+
 void flush_translation_cache_rom(void)
 {
+  u32 used = (u32)(rom_translation_ptr - rom_translation_cache);
+
+  if (used > rom_translation_peak)
+    rom_translation_peak = used;
+  rom_translation_flush_count++;
+
   /* We flush the generated code except for everything below the watermark. */
   last_rom_translation_ptr = &rom_translation_cache[rom_cache_watermark];
   rom_translation_ptr      = &rom_translation_cache[rom_cache_watermark];
@@ -3388,6 +3441,16 @@ void flush_translation_cache_rom(void)
 
 void init_dynarec_caches(void)
 {
+  rom_translation_flush_count = 0;
+  ram_translation_flush_count = 0;
+  ram_translation_capacity_flush_count = 0;
+  ram_translation_store_flush_count = 0;
+  ram_translation_dma_flush_count = 0;
+  rom_translation_peak = 0;
+  ram_translation_peak = 0;
+  jit_cache_sync_calls = 0;
+  jit_cache_sync_failures = 0;
+
   /* Initialize caches so that we can start initalizing the emitter. */
   rom_translation_ptr = last_rom_translation_ptr = &rom_translation_cache[0];
   memset(rom_branch_hash, 0, sizeof(rom_branch_hash));
@@ -3412,4 +3473,3 @@ void flush_dynarec_caches(void)
   iwram_code_max = 0x8000;
   flush_translation_cache_ram();
 }
-
diff --git a/gba_memory.c b/gba_memory.c
index c3a1b29..a1a2dfb 100644
--- a/gba_memory.c
+++ b/gba_memory.c
@@ -19,6 +19,9 @@
 
 #include "common.h"
 #include "streams/file_stream.h"
+#ifdef GPSP_ROM_BUFFER_MMAP
+#include <sys/mman.h>
+#endif
 
 /* Sound */
 #define gbc_sound_tone_control_low(channel, regn)                             \
@@ -362,20 +369,27 @@ u8 gamepak_backup[1024 * 128];
 u32 dma_bus_val;
 dma_transfer_type dma[4];
 
-// ROM memory is allocated in blocks of 1MB to better map the native block
-// mapping system. We will try to allocate 32 of them to allow loading
-// ROMs up to 32MB, but we might fail on memory constrained systems.
+/*
+ * Allocate ROM memory in native 32 KiB mapping pages. NOMMU anonymous
+ * mappings must first obtain a physically contiguous allocation. The old
+ * 1 MiB chunks therefore required a high-order allocation for every chunk
+ * and stopped at 24 MiB even with ample free memory. Page-sized ROM chunks
+ * need only low-order allocations and can materialize a full 32 MiB GBA ROM.
+ */
+#define GAMEPAK_BUFFER_PAGE_SIZE (32 * 1024)
+#define GAMEPAK_MAX_BUFFERS      (32 * 1024 * 1024 / GAMEPAK_BUFFER_PAGE_SIZE)
 
-u8 *gamepak_buffers[32];    /* Pointers to malloc'ed blocks */
-u32 gamepak_buffer_count;   /* Value between 1 and 32 */
+u8 *gamepak_buffers[GAMEPAK_MAX_BUFFERS];
+u32 gamepak_buffer_count;
 u32 gamepak_size;           /* Size of the ROM in bytes */
+u32 gamepak_page_load_count;
+u32 gamepak_page_load_bytes;
 u32 gamepak_file_blocks;    /* Physical payload size in 32KB blocks */
 bool gamepak_mirror_1m;     /* 1MiB Classic NES/Famicom Mini mirror mode */
 static u8 *gamepak_mini_rom;
 bool gamepak_mini_materialized;
 bool gamepak_header_nonstandard;
-// We allocate in 1MB chunks.
-const unsigned gamepak_buffer_blocksize = 1024*1024;
+const unsigned gamepak_buffer_blocksize = GAMEPAK_BUFFER_PAGE_SIZE;
 
 // LRU queue with the loaded blocks and what they map to
 struct {
@@ -1827,11 +1843,14 @@ const dma_region_type dma_region_map[17] =
 #define dma_read_ext(type, tfsize)                                            \
   read_value = read_memory##tfsize(type##_ptr)                                \
 
-#define dma_write_iwram(type, tfsize)                                         \
-  address##tfsize(iwram + 0x8000, type##_ptr & 0x7FFF) =                      \
-                                          eswap##tfsize(read_value);          \
-  if (address##tfsize(iwram, type##_ptr & 0x7FFF))                            \
+#define dma_write_iwram(type, tfsize) {                                       \
+  u32 wraddr = type##_ptr & 0x7FFF;                                           \
+  u##tfsize wrval = eswap##tfsize(read_value);                                \
+  if (address##tfsize(iwram, wraddr) &&                                       \
+      address##tfsize(iwram + 0x8000, wraddr) != wrval)                       \
     alerts |= CPU_ALERT_SMC;                                                  \
+  address##tfsize(iwram + 0x8000, wraddr) = wrval;                            \
+}
 
 #define dma_write_vram(type, tfsize) {                                        \
   u32 wraddr = type##_ptr & 0x1FFFF;                                          \
@@ -1851,10 +1870,14 @@ const dma_region_type dma_region_map[17] =
 #define dma_write_ext(type, tfsize)                                           \
   write_memory##tfsize(type##_ptr, read_value)                                \
 
-#define dma_write_ewram(type, tfsize)                                         \
-  address##tfsize(ewram, type##_ptr & 0x3FFFF) = eswap##tfsize(read_value);   \
-  if (address##tfsize(ewram, (type##_ptr & 0x3FFFF) + 0x40000))               \
+#define dma_write_ewram(type, tfsize) {                                       \
+  u32 wraddr = type##_ptr & 0x3FFFF;                                          \
+  u##tfsize wrval = eswap##tfsize(read_value);                                \
+  if (address##tfsize(ewram, wraddr + 0x40000) &&                             \
+      address##tfsize(ewram, wraddr) != wrval)                                \
     alerts |= CPU_ALERT_SMC;                                                  \
+  address##tfsize(ewram, wraddr) = wrval;                                     \
+}
 
 #define print_line()                                                          \
   dma_print(src_op, dest_op, tfsize);                                         \
@@ -2261,9 +2284,7 @@ u8 *load_gamepak_page(u32 physical_index)
     physical_index %= rom_blocks;
 
   u32 entry = evict_gamepak_page();
-  u32 block_idx = entry / 32;
-  u32 block_off = entry % 32;
-  u8 *swap_location = &gamepak_buffers[block_idx][32 * 1024 * block_off];
+  u8 *swap_location = gamepak_buffers[entry];
 
   // Fill in the entry
   gamepak_blk_queue[entry].phy_rom = physical_index;
@@ -2275,6 +2296,8 @@ u8 *load_gamepak_page(u32 physical_index)
   filestream_seek(gamepak_file_large, file_index * (32 * 1024), SEEK_SET);
   {
     u32 read_len = (u32)filestream_read(gamepak_file_large, swap_location, (32 * 1024));
+    gamepak_page_load_count++;
+    gamepak_page_load_bytes += read_len;
     if (read_len < (32 * 1024))
       memset(swap_location + read_len, 0xFF, (32 * 1024) - read_len);
   }
@@ -2292,11 +2315,22 @@ u8 *load_gamepak_page(u32 physical_index)
 void init_gamepak_buffer(void)
 {
   unsigned i;
-  // Try to allocate up to 32 blocks of 1MB each
+  const u32 target_pages = ROM_BUFFER_SIZE *
+    (1024 * 1024 / GAMEPAK_BUFFER_PAGE_SIZE);
+
+  // Allocate up to ROM_BUFFER_SIZE MiB without high-order NOMMU mappings.
   gamepak_buffer_count = 0;
-  while (gamepak_buffer_count < ROM_BUFFER_SIZE)
+  while (gamepak_buffer_count < target_pages)
   {
+#ifdef GPSP_ROM_BUFFER_MMAP
+    void *ptr = mmap(NULL, gamepak_buffer_blocksize,
+                     PROT_READ | PROT_WRITE,
+                     MAP_SHARED | MAP_ANONYMOUS, -1, 0);
+    if (ptr == MAP_FAILED)
+      ptr = NULL;
+#else
     void *ptr = malloc(gamepak_buffer_blocksize);
+#endif
     if (!ptr)
       break;
     gamepak_buffers[gamepak_buffer_count++] = (u8*)ptr;
@@ -2310,7 +2366,7 @@ void init_gamepak_buffer(void)
   }
 
   gamepak_lru_head = 0;
-  gamepak_lru_tail = 32 * gamepak_buffer_count - 1;
+  gamepak_lru_tail = gamepak_buffer_count - 1;
 }
 
 bool gamepak_must_swap(void)
@@ -2323,6 +2379,12 @@ bool gamepak_must_swap(void)
   return gamepak_buffer_count * gamepak_buffer_blocksize < gamepak_size;
 }
 
+u8 *gamepak_direct_map_base(void)
+{
+  /* Paged on-demand mappings preserve exact cartridge mirroring. */
+  return NULL;
+}
+
 void init_memory(void)
 {
   u32 map_offset = 0, i;
@@ -2394,9 +2462,14 @@ void memory_term(void)
     gamepak_file_large = NULL;
   }
 
   while (gamepak_buffer_count)
   {
+#ifdef GPSP_ROM_BUFFER_MMAP
+    munmap(gamepak_buffers[--gamepak_buffer_count],
+           gamepak_buffer_blocksize);
+#else
     free(gamepak_buffers[--gamepak_buffer_count]);
+#endif
   }
 
   if (gamepak_mini_rom)
@@ -2606,6 +2687,8 @@ static s32 load_gamepak_raw(const char *name)
   unsigned i, j;
   u32 raw_size;
   int64_t fsize;
+  gamepak_page_load_count = 0;
+  gamepak_page_load_bytes = 0;
   gamepak_file_large = filestream_open(name, RETRO_VFS_FILE_ACCESS_READ,
                                        RETRO_VFS_FILE_ACCESS_HINT_NONE);
   if(gamepak_file_large)
@@ -2654,7 +2737,8 @@ static s32 load_gamepak_raw(const char *name)
 
         // Keep buffer[0] in sync with ROM start for header-based logic.
         if (gamepak_buffer_count > 0)
-          memcpy(gamepak_buffers[0], gamepak_mini_rom, gamepak_buffer_blocksize);
+          memcpy(gamepak_buffers[0], gamepak_mini_rom,
+                 gamepak_buffer_blocksize);
 
         map_null(read, 0x8000000, 0xD000000);
         for (phyn = 0; phyn < map_blocks; phyn++)
@@ -2701,10 +2785,13 @@ static s32 load_gamepak_raw(const char *name)
         if (read_len < gamepak_buffer_blocksize)
           memset(gamepak_buffers[i] + read_len, 0xFF, gamepak_buffer_blocksize - read_len);
       }
-      for (j = 0; j < 32 && i*32 + j < gamepak_file_blocks; j++)
+      const u32 pages_per_buffer =
+        gamepak_buffer_blocksize / GAMEPAK_BUFFER_PAGE_SIZE;
+      for (j = 0; j < pages_per_buffer &&
+                  i * pages_per_buffer + j < gamepak_file_blocks; j++)
       {
-        u32 phyn = i*32 + j;
-        u8* blkptr = &gamepak_buffers[i][32 * 1024 * j];
+        u32 phyn = i * pages_per_buffer + j;
+        u8* blkptr = &gamepak_buffers[i][GAMEPAK_BUFFER_PAGE_SIZE * j];
         u32 entry = evict_gamepak_page();
         gamepak_blk_queue[entry].phy_rom = phyn;
         // Map it to the read handlers now
@@ -2928,7 +3015,8 @@ u32 load_gamepak(const struct retro_game_info* info, const char *name,
 
    if (backup_type_reset == BACKUP_UNKN)
    {
-      u32 scan_size = gamepak_size < (1024*1024) ? gamepak_size : (1024*1024);
+      u32 scan_size = gamepak_size < gamepak_buffer_blocksize ?
+                      gamepak_size : gamepak_buffer_blocksize;
       detect_backup_subcircuit(gamepak_buffers[0], scan_size);
    }
 
@@ -2960,6 +3048,13 @@ u32 load_gamepak(const struct retro_game_info* info, const char *name,
 
    if (is_hack && is_pokemon_engine)
    {
+      /*
+       * gba_over.h idle-loop addresses describe the original retail image.
+       * ROM hacks commonly retain the game code while replacing or moving
+       * that code (Unbound's inherited BPRE address is erased padding).
+       * Applying the retail shortcut to unrelated instructions is unsafe.
+       */
+      idle_loop_target_pc = 0xFFFFFFFF;
       backup_type_reset = BACKUP_FLASH;
       flash_bank_cnt = FLASH_SIZE_128KB;
       flash_device_id = FLASH_DEVICE_SANYO_128KB;
diff --git a/gba_memory.h b/gba_memory.h
index 50472a3..65cd7c1 100644
--- a/gba_memory.h
+++ b/gba_memory.h
@@ -253,6 +253,7 @@ s32 load_bios(char *name);
 void init_memory(void);
 void init_gamepak_buffer(void);
 bool gamepak_must_swap(void);
+u8 *gamepak_direct_map_base(void);
 void memory_term(void);
 u8 *load_gamepak_page(u32 physical_index);
 
@@ -261,6 +262,11 @@ extern u32 gbc_sound_wave_update;
 extern dma_transfer_type dma[DMA_CHAN_CNT];
 
 extern u8 open_gba_bios_rom[1024*16];
+extern u32 gamepak_buffer_count;
+extern const unsigned gamepak_buffer_blocksize;
+extern u32 gamepak_page_load_count;
+extern u32 gamepak_page_load_bytes;
+extern u32 gamepak_size;
 extern u16 palette_ram[512];
 extern u16 oam_ram[512];
 extern u16 palette_ram_converted[512];
diff --git a/gpsp_config.h b/gpsp_config.h
index 625f46e..4560387 100644
--- a/gpsp_config.h
+++ b/gpsp_config.h
@@ -12,13 +12,25 @@
 #endif
 
 /* Cache sizes and their config knobs */
+#ifndef ROM_TRANSLATION_CACHE_SIZE
 #if defined(SMALL_TRANSLATION_CACHE)
   #define ROM_TRANSLATION_CACHE_SIZE (1024 * 1024 * 2)
-  #define RAM_TRANSLATION_CACHE_SIZE (1024 * 384)
 #else
   #define ROM_TRANSLATION_CACHE_SIZE (1024 * 1024 * 10)
+#endif
+#endif
+
+#ifndef RAM_TRANSLATION_CACHE_SIZE
+#if defined(SMALL_TRANSLATION_CACHE)
+  #define RAM_TRANSLATION_CACHE_SIZE (1024 * 384)
+#else
   #define RAM_TRANSLATION_CACHE_SIZE (1024 * 512)
 #endif
+#endif
+
+#ifndef TRANSLATOR_WORKSPACE_SIZE
+  #define TRANSLATOR_WORKSPACE_SIZE 0
+#endif
 
 /* Should be an upperbound to the maximum number of bytes a single JIT'ed
    instruction can take. STM/LDM are tipically the biggest ones */
diff --git a/libretro/libretro.c b/libretro/libretro.c
index 46e958a..8b38b48 100644
--- a/libretro/libretro.c
+++ b/libretro/libretro.c
@@ -620,8 +620,20 @@ void retro_init(void)
 
 #if defined(HAVE_DYNAREC)
   #if defined(MMAP_JIT_CACHE)
-   rom_translation_cache = map_jit_block(ROM_TRANSLATION_CACHE_SIZE + RAM_TRANSLATION_CACHE_SIZE);
+   rom_translation_cache = map_jit_block(ROM_TRANSLATION_CACHE_SIZE +
+                                         RAM_TRANSLATION_CACHE_SIZE +
+                                         TRANSLATOR_WORKSPACE_SIZE);
+   if (!rom_translation_cache)
+   {
+      if (log_cb)
+         log_cb(RETRO_LOG_ERROR,
+                "Failed to allocate executable translation cache\n");
+      dynarec_enable = 0;
+      return;
+   }
    ram_translation_cache = &rom_translation_cache[ROM_TRANSLATION_CACHE_SIZE];
+   rom_translation_ptr = rom_translation_cache;
+   ram_translation_ptr = ram_translation_cache;
   #elif defined(_3DS)
    if (__ctr_svchax && !translation_caches_inited)
    {
@@ -722,7 +734,8 @@ void retro_deinit(void)
    memory_term();
 
 #if defined(MMAP_JIT_CACHE) && defined(HAVE_DYNAREC)
-   unmap_jit_block(rom_translation_cache, ROM_TRANSLATION_CACHE_SIZE + RAM_TRANSLATION_CACHE_SIZE);
+   unmap_jit_block(rom_translation_cache, ROM_TRANSLATION_CACHE_SIZE +
+                   RAM_TRANSLATION_CACHE_SIZE + TRANSLATOR_WORKSPACE_SIZE);
 #endif
 #if defined(_3DS) && defined(HAVE_DYNAREC)
 
@@ -1241,6 +1254,13 @@ bool retro_load_game(const struct retro_game_info* info)
       error_msg("Could not load the game file.");
       return false;
    }
+   if (log_cb)
+      log_cb(RETRO_LOG_INFO,
+            "[gpSP ROM] size=%u buffer_mib=%u swapped=%u direct=%u\n",
+            gamepak_size,
+            (gamepak_buffer_count * gamepak_buffer_blocksize) / (1024 * 1024),
+            gamepak_must_swap() ? 1u : 0u,
+            gamepak_direct_map_base() ? 1u : 0u);
 
    if (selected_bios != builtin_bios && gamepak_header_nonstandard)
    {
@@ -1286,6 +1306,38 @@ bool retro_load_game_special(unsigned game_type,
 
 void retro_unload_game(void)
 {
+#ifdef HAVE_DYNAREC
+   u32 rom_used = (u32)(rom_translation_ptr - rom_translation_cache);
+   u32 ram_used = (u32)(ram_translation_ptr - ram_translation_cache);
+
+   if (rom_used > rom_translation_peak)
+      rom_translation_peak = rom_used;
+   if (ram_used > ram_translation_peak)
+      ram_translation_peak = ram_used;
+   if (log_cb) {
+      log_cb(RETRO_LOG_INFO,
+            "[gpSP JIT] rom_peak=%u rom_capacity=%u rom_flushes=%u "
+            "ram_peak=%u ram_capacity=%u ram_flushes=%u\n",
+            rom_translation_peak, (u32)ROM_TRANSLATION_CACHE_SIZE,
+            rom_translation_flush_count,
+            ram_translation_peak, (u32)RAM_TRANSLATION_CACHE_SIZE,
+            ram_translation_flush_count);
+      log_cb(RETRO_LOG_INFO,
+            "[gpSP JIT reason] capacity=%u store=%u dma=%u\n",
+            ram_translation_capacity_flush_count,
+            ram_translation_store_flush_count,
+            ram_translation_dma_flush_count);
+      log_cb(RETRO_LOG_INFO,
+            "[gpSP JIT cache] calls=%u failures=%u\n",
+            jit_cache_sync_calls, jit_cache_sync_failures);
+   }
+#endif
+
+   if (log_cb)
+      log_cb(RETRO_LOG_INFO,
+            "[gpSP ROM] runtime_page_loads=%u runtime_page_bytes=%u\n",
+            gamepak_page_load_count, gamepak_page_load_bytes);
+
    if (libretro_ff_enabled)
       set_fastforward_override(false);
 
diff --git a/main.c b/main.c
index 6fb26fc..8031fbe 100644
--- a/main.c
+++ b/main.c
@@ -452,4 +452,3 @@ unsigned main_write_savestate(u8* dst)
 
   return (unsigned int)(dst - startp);
 }
-
diff --git a/memmap.c b/memmap.c
index 46bb951..e59d6b9 100644
--- a/memmap.c
+++ b/memmap.c
@@ -29,7 +29,7 @@
   #define _MAP_ITERATIONS           1024   // Test -/+2GB in 4MB steps
   #define _MAP_STEP         (4*1024*1024)
   #define _VALIDATE_BLOCK_FN(ptr, size) \
-          validate_addr_section_mips(ptr, size)
+          validate_addr_section_mips(ptr, size, 0)
 #elif defined(ARM64_ARCH)
   #define _MAP_ITERATIONS            256   // Test -/+128MB in 1MB steps
   #define _MAP_STEP           (1024*1024)
diff --git a/mips/mips_emit.h b/mips/mips_emit.h
index 79d5183..8077a90 100644
--- a/mips/mips_emit.h
+++ b/mips/mips_emit.h
@@ -51,6 +51,7 @@ void mips_cheat_hook(void);
 u32 execute_spsr_restore(u32 address);
 void execute_store_cpsr(u32 new_cpsr, u32 store_mask);
 void execute_store_spsr(u32 new_spsr, u32 store_mask);
+void platform_cache_sync(void *baseaddr, void *endptr);
 
 
 #define reg_base    mips_reg_s0
@@ -1512,9 +1513,10 @@ u32 execute_store_cpsr_body(u32 _cpsr, u32 address)
 #define thumb_data_proc_mov_hi()                                              \
 {                                                                             \
   thumb_decode_hireg_op();                                                    \
+  u32 dest_rd = rd;                                                           \
   check_load_reg_pc(arm_reg_a0, rs, 4);                                       \
-  mips_emit_addu(arm_to_mips_reg[rd], arm_to_mips_reg[rs], reg_zero);         \
-  check_store_reg_pc_thumb(rd);                                               \
+  mips_emit_addu(arm_to_mips_reg[dest_rd], arm_to_mips_reg[rs], reg_zero);    \
+  check_store_reg_pc_thumb(dest_rd);                                          \
 }                                                                             \
 
 #define thumb_load_pc(_rd)                                                    \
@@ -1969,6 +1971,7 @@ u32 execute_store_cpsr_body(u32 _cpsr, u32 address)
 #define ReOff_SaveR1   (REG_SAVE  * 4) // 3 save scratch regs
 #define ReOff_SaveR2   (REG_SAVE2 * 4)
 #define ReOff_SaveR3   (REG_SAVE3 * 4)
+#define ReOff_SaveR4   (REG_SAVE4 * 4)
 #define ReOff_OamUpd   (OAM_UPDATED*4) // OAM_UPDATED
 #define ReOff_GP_Save  (REG_SAVE5 * 4) // GP_SAVE
 
@@ -2067,7 +2070,7 @@ static void emit_mem_access_loadop(
   };
 }
 
-#ifdef PIC
+#if defined(PIC) || defined(__PIC__)
   #define genccall(fn)                                         \
     mips_emit_lui(mips_reg_t9, ((u32)fn) >> 16);               \
     mips_emit_ori(mips_reg_t9, mips_reg_t9, ((u32)fn));        \
@@ -2157,33 +2160,45 @@ static void emit_pmemld_stub(
   }
   
   if (region >= 8 && region <= 12) {
-    // ROM area: might need to load the ROM on-demand
-    mips_emit_srl(reg_rv, reg_a0, 15);  // 32KB page number
-    mips_emit_sll(reg_rv, reg_rv,  2);  // (word indexed)
-    mips_emit_addu(reg_rv, reg_rv, reg_base);    // base + offset
-    mips_emit_lw(reg_rv, reg_rv, 0x8000);        // base[offset-0x8000] is readmap ptr
-    mips_emit_andi(reg_temp, reg_a0, memmask);   // Get the lowest 15 bits [can go in delay slot]
-
-    if (must_swap) {   // Do not emit if the ROM is fully loaded, save some cycles
-      u8 *jmppatch;
-      mips_emit_b_filler(bne, reg_rv, reg_zero, jmppatch);  // if not null, can skip load page
-      generate_swap_delay();
-
-      // This code call the C routine to map the relevant ROM page
-      emit_save_regs(aligned);
-      mips_emit_sw(mips_reg_ra, reg_base, ReOff_SaveR3);
-      extract_bits(reg_a0, reg_a0, 15, 10);    // a0 = (addr >> 15) & 0x3ff
-      genccall(&load_gamepak_page);            // Returns valid pointer in rv
-      mips_emit_sw(reg_temp, reg_base, ReOff_SaveR1);
-
-      mips_emit_lw(reg_temp, reg_base, ReOff_SaveR1);
-      emit_restore_regs(aligned);
-      mips_emit_lw(mips_reg_ra, reg_base, ReOff_SaveR3);
-
-      generate_branch_patch_conditional(jmppatch - 4, translation_ptr);
+    u8 *direct_base = gamepak_direct_map_base();
+    if (direct_base) {
+      /*
+       * A 32 MiB ROM in the board's aligned contiguous mapping can use the
+       * GBA bus address directly. This removes readmap lookup and its dependent
+       * load from every generated cartridge data access.
+       */
+      mips_emit_sll(reg_rv, reg_a0, 7);
+      mips_emit_srl(reg_rv, reg_rv, 7);
+      generate_load_imm(reg_temp, (u32)direct_base);
+      mips_emit_addu(reg_rv, reg_rv, reg_temp);
+    } else {
+      // ROM area: might need to load the ROM on-demand
+      mips_emit_srl(reg_rv, reg_a0, 15);  // 32KB page number
+      mips_emit_sll(reg_rv, reg_rv,  2);  // (word indexed)
+      mips_emit_addu(reg_rv, reg_rv, reg_base);    // base + offset
+      mips_emit_lw(reg_rv, reg_rv, 0x8000);        // base[offset-0x8000] is readmap ptr
+      mips_emit_andi(reg_temp, reg_a0, memmask);   // Get the lowest 15 bits
+
+      if (must_swap) {   // Do not emit if the ROM is fully loaded, save some cycles
+        u8 *jmppatch;
+        mips_emit_b_filler(bne, reg_rv, reg_zero, jmppatch);
+        generate_swap_delay();
+
+        // This code calls the C routine to map the relevant ROM page.
+        emit_save_regs(aligned);
+        mips_emit_sw(mips_reg_ra, reg_base, ReOff_SaveR3);
+        extract_bits(reg_a0, reg_a0, 15, 10);
+        genccall(&load_gamepak_page);
+        mips_emit_sw(reg_temp, reg_base, ReOff_SaveR1);
+
+        mips_emit_lw(reg_temp, reg_base, ReOff_SaveR1);
+        emit_restore_regs(aligned);
+        mips_emit_lw(mips_reg_ra, reg_base, ReOff_SaveR3);
+
+        generate_branch_patch_conditional(jmppatch - 4, translation_ptr);
+      }
+      mips_emit_addu(reg_rv, reg_rv, reg_temp);
     }
-    // Now we can proceed to load, place addr in the right register
-    mips_emit_addu(reg_rv, reg_rv, reg_temp);
   } else if (region == 14) {
     // Read from flash, is a bit special, fn call
     emit_mem_call_ds(&read_backup, 0xFFFF);
@@ -2254,6 +2269,8 @@ static void emit_pmemst_stub(
   unsigned size, bool aligned, u8 **tr_ptr)
 {
   u8 *translation_ptr = *tr_ptr;
+  u8 *smc_no_code_branch = NULL;
+  u8 *smc_same_value_branch = NULL;
   unsigned region = meminfo->region;
   u32 base_addr = meminfo->baseptr;
 
@@ -2323,12 +2340,41 @@ static void emit_pmemst_stub(
     } else {
       mips_emit_lb(reg_temp, reg_temp, base_addr);
     }
-    // If the data is non zero, we just wrote over code
-    // Local-jump to the smc_write (which lives at offset:0)
-    mips_emit_b(bne, reg_zero, reg_temp, branch_offset(&rom_translation_cache[SMC_WRITE_OFF]));
+    /*
+     * A write of the value already in RAM does not modify translated code.
+     * Games which repeatedly install the same IWRAM helper would otherwise
+     * discard every RAM translation on every identical copy.
+     */
+    smc_no_code_branch = translation_ptr;
+    mips_emit_b(beq, reg_zero, reg_temp, 0);
+    if (realsize == 2) {
+      mips_emit_addu(reg_a0, reg_a1, reg_zero);
+    } else if (realsize == 1) {
+      mips_emit_andi(reg_a0, reg_a1, 0xffff);
+    } else {
+      mips_emit_andi(reg_a0, reg_a1, 0xff);
+    }
+
+    if (realsize == 2) {
+      mips_emit_lw(reg_temp, reg_rv, base_addr);
+    } else if (realsize == 1) {
+      mips_emit_lhu(reg_temp, reg_rv, base_addr);
+    } else {
+      mips_emit_lbu(reg_temp, reg_rv, base_addr);
+    }
+
+    smc_same_value_branch = translation_ptr;
+    mips_emit_b(beq, reg_temp, reg_a0, 0);
+    mips_emit_nop();
+
+    // A changed code byte must still take the full SMC invalidation path.
+    mips_emit_b(beq, reg_zero, reg_zero,
+                branch_offset(&rom_translation_cache[SMC_WRITE_OFF]));
   }
 
-  // Store the data (delay slot from the SMC branch)
+  // Store changed data in the SMC branch delay slot, or ordinary RAM here.
+  if (smc_no_code_branch)
+    generate_branch_patch_conditional(smc_no_code_branch, translation_ptr);
   if (realsize == 2) {
     mips_emit_sw(reg_a1, reg_rv, base_addr);
   } else if (realsize == 1) {
@@ -2345,6 +2391,8 @@ static void emit_pmemst_stub(
     generate_function_return_swap_delay();
   }
   else {
+    if (smc_same_value_branch)
+      generate_branch_patch_conditional(smc_same_value_branch, translation_ptr);
     mips_emit_jr(mips_reg_ra);
     mips_emit_nop();
   }
@@ -2653,13 +2701,26 @@ static void emit_phand(
   mips_emit_addu(reg_temp, reg_temp, reg_base); // Add to the base_reg the table offset
   mips_emit_lw(reg_rv,   reg_temp, tbloff);     // Get func addr from 1st table
   mips_emit_lw(reg_temp, reg_temp, tbloff2);    // Get opcode from 2nd table
-  mips_emit_sw(reg_temp, mips_reg_ra, -8);      // Patch instruction!
-
   #if defined(PSP)
+    mips_emit_sw(reg_temp, mips_reg_ra, -8);    // Patch instruction!
     mips_emit_cache(0x1A, mips_reg_ra, -8);
     mips_emit_jr(reg_rv);                       // Jump directly to target for speed
     mips_emit_cache(0x08, mips_reg_ra, -8);
+  #elif defined(GPSP_DYNAREC_SAFE_SMC_PATCH)
+    /*
+     * Linux/MIPS does not guarantee I/D cache coherence after a user-space
+     * code write. Mark the cache dirty and publish all patches at the frame
+     * boundary, when no translated code is executing. Both the old generic
+     * call and new specialised call are valid until then.
+    */
+    mips_emit_sw(reg_temp, mips_reg_ra, -8);    // Patch instruction!
+    mips_emit_jr(reg_rv);
+    mips_emit_sw(reg_base, reg_base, ReOff_SaveR4);
+  #elif defined(GPSP_DYNAREC_NO_SMC_PATCH)
+    mips_emit_jr(reg_rv);
+    mips_emit_nop();
   #else
+    mips_emit_sw(reg_temp, mips_reg_ra, -8);    // Patch instruction!
     mips_emit_jr(reg_rv);
     #ifdef MIPS_HAS_R2_INSTS
       mips_emit_synci(mips_reg_ra, -8);
@@ -2805,9 +2866,22 @@ void init_emitter(bool must_swap) {
 
 u32 execute_arm_translate_internal(u32 cycles, void *regptr);
 u32 execute_arm_translate(u32 cycles) {
-  return execute_arm_translate_internal(cycles, &reg[0]);
+  u32 ret = execute_arm_translate_internal(cycles, &reg[0]);
+  if (reg[REG_SAVE4]) {
+    platform_cache_sync(rom_translation_cache, rom_translation_ptr);
+    platform_cache_sync(ram_translation_cache, ram_translation_ptr);
+    last_rom_translation_ptr = rom_translation_ptr;
+    last_ram_translation_ptr = ram_translation_ptr;
+    reg[REG_SAVE4] = 0;
+  }
+#ifdef GPSP_DYNAREC_SAFE_FALLBACK
+  if (ret == GPSP_DYNAREC_LOOKUP_FAILED) {
+    clear_gamepak_stickybits();
+    execute_arm(cycles);
+    return 0;
+  }
+#endif
+  return ret;
 }
 
 #endif
-
-
diff --git a/mips/mips_stub.S b/mips/mips_stub.S
index 34b0446..6619c8c 100644
--- a/mips/mips_stub.S
+++ b/mips/mips_stub.S
@@ -18,17 +18,41 @@
 
 #include "../gpsp_config.h"
 
+#ifdef GPSP_DYNAREC_SAFE_FALLBACK
+#define GPSP_DYNAREC_LOOKUP_FAILED 0x7ffffffe
+#endif
+
 // This is also defined in sys/asm.h but doesn't seem portable?
 #ifdef __mips64
   .set mips64
   #define SZREG 8
   #define REG_L ld
   #define REG_S sd
-#else
+#elif defined(MIPS_HAS_R2_INSTS)
   .set mips32r2
   #define SZREG 4
   #define REG_L lw
   #define REG_S sw
+#else
+  .set mips32
+  #define SZREG 4
+  #define REG_L lw
+  #define REG_S sw
+#endif
+
+#ifdef GPSP_DYNAREC_SAFE_FALLBACK
+.macro jump_translated_result
+  beqz $2, translated_lookup_failed
+  addiu $1, $0, -4
+  and $2, $2, $1
+  jr $2
+  nop
+.endm
+#else
+.macro jump_translated_result
+  jr $2
+  nop
+.endm
 #endif
 
 #define defsymbl(symbol) \
@@ -214,7 +238,7 @@
 
 # PIC ABI mandates to jump to target via $t9
 
-#ifdef PIC
+#if defined(PIC) || defined(__PIC__)
 .macro cfncall target, targetid
   lw $t9, (FNPTRS_BASE + \targetid * 4)($16)
   jalr $t9
@@ -298,23 +322,29 @@ defsymbl(mips_indirect_branch_arm)
   save_registers
   cfncall block_lookup_address_arm, 1
   restore_registers
-  jr $2                           # $2 = value returned
-  nop
+  jump_translated_result          # $2 = value returned
 
 defsymbl(mips_indirect_branch_thumb)
   save_registers
   cfncall block_lookup_address_thumb, 2
   restore_registers
-  jr $2                           # $2 = value returned
-  nop
+  jump_translated_result          # $2 = value returned
 
 defsymbl(mips_indirect_branch_dual)
   save_registers
   cfncall block_lookup_address_dual, 3
   nop
   restore_registers
-  jr $2                           # $2 = value returned
+  jump_translated_result          # $2 = value returned
+
+#ifdef GPSP_DYNAREC_SAFE_FALLBACK
+translated_lookup_failed:
+  collapse_flags
+  save_registers
+  li $2, GPSP_DYNAREC_LOOKUP_FAILED
+  b return_to_main
   nop
+#endif
 
 
 # Called on I/O writes that have side-effects
@@ -326,7 +356,7 @@ defsymbl(write_io_epilogue)
   andi $4, $19, CPU_ALERT_SMC     # check if SMC code happened
   beqz $4, 1f                     # skip if no SMC happened
   nop
-  cfncall flush_translation_cache_ram, 4
+  cfncall flush_translation_cache_ram_dma, 11
 
 1:
   andi $4, $19, CPU_ALERT_IRQ     # check if IRQ was raised
@@ -359,19 +389,17 @@ lookup_pc_noflags:
 
   cfncall block_lookup_address_thumb, 2  # get Thumb address
   restore_registers
-  jr $2                                  # jump to result
-  nop
+  jump_translated_result                 # jump to result
 1:
   cfncall block_lookup_address_arm, 1  # get ARM address
   restore_registers
-  jr $2                                # jump to result
-  nop
+  jump_translated_result               # jump to result
 
 
 defsymbl(smc_write)
   sw $6, REG_PC($16)              # save PC
   save_registers
-  cfncall flush_translation_cache_ram, 4
+  cfncall flush_translation_cache_ram_store, 10
   b lookup_pc_noflags
   lw $1, REG_CPSR($16)            # (delay)
 
@@ -485,8 +513,7 @@ defsymbl(execute_store_cpsr)
   addu $4, $2, $0                      # load new address in $4
   cfncall block_lookup_address_arm, 1  # GBA address is in $4
   restore_registers                    # restore registers
-  jr $2                                # jump to the new address
-  nop
+  jump_translated_result               # jump to the new address
 
 
 # $4: new spsr
@@ -542,14 +569,12 @@ defsymbl(execute_arm_translate_internal)
 
   cfncall block_lookup_address_arm, 1
   restore_registers               # load initial register values
-  jr $2                           # jump to return
-  nop
+  jump_translated_result          # jump to return
 
 1:
   cfncall block_lookup_address_thumb, 2
   restore_registers               # load initial register values
-  jr $2                           # jump to return
-  nop
+  jump_translated_result          # jump to return
 
 .bss
 .align 6
@@ -610,6 +635,8 @@ fnptrs:
   .long execute_store_cpsr_body        # 7
   .long process_cheats                 # 8
   .long check_and_raise_interrupts     # 9
+  .long flush_translation_cache_ram_store # 10
+  .long flush_translation_cache_ram_dma # 11
 
 #if !defined(MMAP_JIT_CACHE)
 
@@ -633,4 +660,3 @@ defsymbl(ram_translation_cache)
 #if defined(__linux__) && defined(__ELF__)
   .section .note.GNU-stack,"",%progbits
 #endif
-
