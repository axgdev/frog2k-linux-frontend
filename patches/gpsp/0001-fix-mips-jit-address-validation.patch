diff --git a/Makefile.common b/Makefile.common
index 9742c05..608275f 100644
--- a/Makefile.common
+++ b/Makefile.common
@@ -23,7 +23,6 @@ SOURCES_C := $(CORE_DIR)/main.c \
              $(LIBRETRO_COMM_DIR)/compat/compat_posix_string.c \
              $(LIBRETRO_COMM_DIR)/compat/compat_strl.c \
              $(LIBRETRO_COMM_DIR)/compat/fopen_utf8.c \
-             $(LIBRETRO_COMM_DIR)/encodings/encoding_utf.c \
              $(LIBRETRO_COMM_DIR)/file/file_path.c \
              $(LIBRETRO_COMM_DIR)/file/file_path_io.c \
              $(LIBRETRO_COMM_DIR)/streams/file_stream.c \
@@ -44,4 +43,3 @@ ifeq ($(HAVE_DYNAREC), 1)
   endif
 endif
 
-
diff --git a/common.h b/common.h
index d9a0f1d..af8c088 100644
--- a/common.h
+++ b/common.h
@@ -40,6 +40,10 @@
   #endif
 #endif
 
+#ifdef SF2000
+#define SF2000_GPSP_DYNAREC_LOOKUP_FAILED 0x7ffffffeu
+#endif
+
 #define ror(dest, value, shift)                                               \
   dest = ((value) >> (shift)) | ((value) << (32 - (shift)))                   \
 
diff --git a/cpu_threaded.c b/cpu_threaded.c
index 8942ab3..d86e80b 100644
--- a/cpu_threaded.c
+++ b/cpu_threaded.c
@@ -2653,12 +2653,15 @@ u8 function_cc *block_lookup_address_arm(u32 pc)
   unsigned i;
   for (i = 0; i < 4; i++) {
     u8 *ret = block_lookup_translate_arm(pc);
-    if (ret) {
+    if (ret && ret != (u8 *)(~0)) {
       translate_icache_sync();
       return ret;
     }
+    if (ret == (u8 *)(~0))
+      break;
   }
 
+  reg[REG_PC] = pc;
   printf("bad jump %x (%x)\n", pc, reg[REG_PC]);
   fflush(stdout);
   return NULL;
@@ -2669,11 +2672,14 @@ u8 function_cc *block_lookup_address_thumb(u32 pc)
   unsigned i;
   for (i = 0; i < 4; i++) {
     u8 *ret = block_lookup_translate_thumb(pc);
-    if (ret) {
+    if (ret && ret != (u8 *)(~0)) {
       translate_icache_sync();
       return ret;
     }
+    if (ret == (u8 *)(~0))
+      break;
   }
+  reg[REG_PC] = pc;
   printf("bad jump %x (%x)\n", pc, reg[REG_PC]);
   fflush(stdout);
   return NULL;
@@ -3412,4 +3418,3 @@ void flush_dynarec_caches(void)
   iwram_code_max = 0x8000;
   flush_translation_cache_ram();
 }
-
diff --git a/gpsp_config.h b/gpsp_config.h
index 625f46e..33b5193 100644
--- a/gpsp_config.h
+++ b/gpsp_config.h
@@ -12,7 +12,10 @@
 #endif
 
 /* Cache sizes and their config knobs */
-#if defined(SMALL_TRANSLATION_CACHE)
+#if defined(SF2000_NOMMU)
+  #define ROM_TRANSLATION_CACHE_SIZE (1024 * 512)
+  #define RAM_TRANSLATION_CACHE_SIZE (1024 * 128)
+#elif defined(SMALL_TRANSLATION_CACHE)
   #define ROM_TRANSLATION_CACHE_SIZE (1024 * 1024 * 2)
   #define RAM_TRANSLATION_CACHE_SIZE (1024 * 384)
 #else
diff --git a/memmap.c b/memmap.c
index 46bb951..41cc27c 100644
--- a/memmap.c
+++ b/memmap.c
@@ -29,7 +29,7 @@
   #define _MAP_ITERATIONS           1024   // Test -/+2GB in 4MB steps
   #define _MAP_STEP         (4*1024*1024)
   #define _VALIDATE_BLOCK_FN(ptr, size) \
-          validate_addr_section_mips(ptr, size)
+          validate_addr_section_mips(ptr, size, 0)
 #elif defined(ARM64_ARCH)
   #define _MAP_ITERATIONS            256   // Test -/+128MB in 1MB steps
   #define _MAP_STEP           (1024*1024)
@@ -102,6 +102,11 @@ bool validate_addr_section_mips(void *ptr, unsigned size, unsigned max_offset_mb
 
 	// Posix implementation
 	void *map_jit_block(unsigned size) {
+#if defined(SF2000_NOMMU)
+		void *p = mmap((void *)0, size, PROT_READ|PROT_WRITE|PROT_EXEC,
+		                    MAP_ANON|MAP_SHARED, -1, 0);
+		return p == MAP_FAILED ? 0 : p;
+#else
 		unsigned i;
 		uintptr_t base = (uintptr_t)(map_jit_block) & (~(_MAP_STEP - 1ULL));
 		for (i = 0; i < _MAP_ITERATIONS; i++) {
@@ -120,6 +125,7 @@ bool validate_addr_section_mips(void *ptr, unsigned size, unsigned max_offset_mb
 			}
 		}
 		return 0;
+#endif
 	}
 
 	void unmap_jit_block(void *bufptr, unsigned size) {
@@ -129,4 +135,3 @@ bool validate_addr_section_mips(void *ptr, unsigned size, unsigned max_offset_mb
 #endif /* WIN32 */
 
 #endif /* MMAP_JIT_CACHE */
-
diff --git a/mips/mips_emit.h b/mips/mips_emit.h
index 79d5183..0b91040 100644
--- a/mips/mips_emit.h
+++ b/mips/mips_emit.h
@@ -2653,13 +2653,16 @@ static void emit_phand(
   mips_emit_addu(reg_temp, reg_temp, reg_base); // Add to the base_reg the table offset
   mips_emit_lw(reg_rv,   reg_temp, tbloff);     // Get func addr from 1st table
   mips_emit_lw(reg_temp, reg_temp, tbloff2);    // Get opcode from 2nd table
-  mips_emit_sw(reg_temp, mips_reg_ra, -8);      // Patch instruction!
-
   #if defined(PSP)
+    mips_emit_sw(reg_temp, mips_reg_ra, -8);    // Patch instruction!
     mips_emit_cache(0x1A, mips_reg_ra, -8);
     mips_emit_jr(reg_rv);                       // Jump directly to target for speed
     mips_emit_cache(0x08, mips_reg_ra, -8);
+  #elif defined(SF2000)
+    mips_emit_jr(reg_rv);
+    mips_emit_nop();
   #else
+    mips_emit_sw(reg_temp, mips_reg_ra, -8);    // Patch instruction!
     mips_emit_jr(reg_rv);
     #ifdef MIPS_HAS_R2_INSTS
       mips_emit_synci(mips_reg_ra, -8);
@@ -2801,13 +2804,23 @@ void init_emitter(bool must_swap) {
   rom_cache_watermark = (u32)(translation_ptr - rom_translation_cache);
 
   init_bios_hooks();
+
+  /* Permanent helper stubs precede the normal cache-sync watermark. */
+  __builtin___clear_cache((char *)rom_translation_cache,
+                          (char *)rom_translation_ptr);
 }
 
 u32 execute_arm_translate_internal(u32 cycles, void *regptr);
 u32 execute_arm_translate(u32 cycles) {
-  return execute_arm_translate_internal(cycles, &reg[0]);
+  u32 ret = execute_arm_translate_internal(cycles, &reg[0]);
+#ifdef SF2000
+  if (ret == SF2000_GPSP_DYNAREC_LOOKUP_FAILED) {
+    clear_gamepak_stickybits();
+    execute_arm(cycles);
+    return 0;
+  }
+#endif
+  return ret;
 }
 
 #endif
-
-
diff --git a/mips/mips_stub.S b/mips/mips_stub.S
index 34b0446..be0a9df 100644
--- a/mips/mips_stub.S
+++ b/mips/mips_stub.S
@@ -18,19 +18,34 @@
 
 #include "../gpsp_config.h"
 
+#define SF2000_GPSP_DYNAREC_LOOKUP_FAILED 0x7ffffffe
+
 // This is also defined in sys/asm.h but doesn't seem portable?
 #ifdef __mips64
   .set mips64
   #define SZREG 8
   #define REG_L ld
   #define REG_S sd
-#else
+#elif defined(MIPS_HAS_R2_INSTS)
   .set mips32r2
   #define SZREG 4
   #define REG_L lw
   #define REG_S sw
+#else
+  .set mips32
+  #define SZREG 4
+  #define REG_L lw
+  #define REG_S sw
 #endif
 
+.macro jump_translated_result
+  beqz $2, translated_lookup_failed
+  addiu $1, $0, -4
+  and $2, $2, $1
+  jr $2
+  nop
+.endm
+
 #define defsymbl(symbol) \
 .type symbol, %function ;\
 .global symbol ;         \
@@ -298,22 +313,26 @@ defsymbl(mips_indirect_branch_arm)
   save_registers
   cfncall block_lookup_address_arm, 1
   restore_registers
-  jr $2                           # $2 = value returned
-  nop
+  jump_translated_result          # $2 = value returned
 
 defsymbl(mips_indirect_branch_thumb)
   save_registers
   cfncall block_lookup_address_thumb, 2
   restore_registers
-  jr $2                           # $2 = value returned
-  nop
+  jump_translated_result          # $2 = value returned
 
 defsymbl(mips_indirect_branch_dual)
   save_registers
   cfncall block_lookup_address_dual, 3
   nop
   restore_registers
-  jr $2                           # $2 = value returned
+  jump_translated_result          # $2 = value returned
+
+translated_lookup_failed:
+  collapse_flags
+  save_registers
+  li $2, SF2000_GPSP_DYNAREC_LOOKUP_FAILED
+  b return_to_main
   nop
 
 
@@ -359,13 +378,11 @@ lookup_pc_noflags:
 
   cfncall block_lookup_address_thumb, 2  # get Thumb address
   restore_registers
-  jr $2                                  # jump to result
-  nop
+  jump_translated_result                 # jump to result
 1:
   cfncall block_lookup_address_arm, 1  # get ARM address
   restore_registers
-  jr $2                                # jump to result
-  nop
+  jump_translated_result               # jump to result
 
 
 defsymbl(smc_write)
@@ -485,8 +502,7 @@ defsymbl(execute_store_cpsr)
   addu $4, $2, $0                      # load new address in $4
   cfncall block_lookup_address_arm, 1  # GBA address is in $4
   restore_registers                    # restore registers
-  jr $2                                # jump to the new address
-  nop
+  jump_translated_result               # jump to the new address
 
 
 # $4: new spsr
@@ -542,14 +558,12 @@ defsymbl(execute_arm_translate_internal)
 
   cfncall block_lookup_address_arm, 1
   restore_registers               # load initial register values
-  jr $2                           # jump to return
-  nop
+  jump_translated_result          # jump to return
 
 1:
   cfncall block_lookup_address_thumb, 2
   restore_registers               # load initial register values
-  jr $2                           # jump to return
-  nop
+  jump_translated_result          # jump to return
 
 .bss
 .align 6
@@ -615,7 +629,7 @@ fnptrs:
 
 # Make this section executable!
 .text
-#if defined(PSP) || defined(PS2)
+#if defined(PSP) || defined(PS2) || defined(SF2000_NOMMU)
   .section .bss
 #else
   # Need to mark the section as awx (for Linux)
@@ -633,4 +647,3 @@ defsymbl(ram_translation_cache)
 #if defined(__linux__) && defined(__ELF__)
   .section .note.GNU-stack,"",%progbits
 #endif
-
